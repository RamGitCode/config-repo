spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/user_master
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: flase
