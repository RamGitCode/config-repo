spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/gateway_master
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: flase
